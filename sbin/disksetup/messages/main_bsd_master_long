The default master boot record provided with BSD/OS is bootany.sys.
To use  bootany.sys, simply press <ENTER>, else provide the name
of the file which contains the master boot record you wish to use.

